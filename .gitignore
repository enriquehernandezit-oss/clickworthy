# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# design-skill tool artifacts (emilkowalski/skills — local reference, not app code)
.agents/
skills-lock.json

# scratch scripts + downloaded service-account keys — never commit
tmp-*.ts
*service-account*.json
*.serviceaccount.json

# Stray World Bank 404 page mis-saved as .xlsx, committed by accident (0480f24)
wbes_innov.xlsx

# Playwright MCP scratch output (accessibility snapshots from browser testing)
.playwright-mcp/
